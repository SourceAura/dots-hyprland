# SiM Syndicate — Backend Dependencies
# Minimal FastAPI stack. No Redis, no Celery, no Docker SDK, no PyTorch.

# Web framework
fastapi
uvicorn[standard]
pydantic
python-multipart
aiofiles

# HTTP client (Ollama streaming)
aiohttp

# Database (SQLite async)
sqlalchemy
aiosqlite
databases

# Vector memory (Wisteria)
chromadb

# System monitoring
psutil

# Utilities
python-dotenv
loguru
nest-asyncio
python-dateutil
typing-extensions
pyyaml

# WebSocket server (bridge processes)
websockets>=12.0

# PTY (Codex terminal bridge — Ghostty detach only, kept for inline ROM)
ptyprocess
pyte
